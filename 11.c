#include <stdio.h>

int main ()
{
	int cand1 = 0, cand2 = 0, branco = 0, voto;

	for(i = 0; i < 100; i++ )
	{
		voto = 0;
		scanf("%d", &voto);
		if (voto == )
		if (voto == 83)
			cand1 ++;
		else if (voto == 93)
			cand2 ++;
		else if (voto = 00)
			branco++;
		else if (voto != 83 || voto != 93|| voto != 00)
		{
			break;
		}


	}

	return 0;
}